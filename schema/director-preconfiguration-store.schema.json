{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local-video.invalid/schema/director-preconfiguration-store.json",
  "type": "object", "additionalProperties": false, "required": ["version", "entries"],
  "properties": {
    "version": { "enum": [1, 2] },
    "entries": { "type": "array", "maxItems": 100, "items": { "type": "object", "additionalProperties": false, "required": ["revision", "createdAt", "updatedAt", "preconfiguration"], "properties": { "revision": { "type": "integer", "minimum": 1 }, "createdAt": { "type": "string", "format": "date-time" }, "updatedAt": { "type": "string", "format": "date-time" }, "preconfiguration": { "$ref": "https://local-video.invalid/schema/director-preconfiguration.json" }, "legacyPreconfiguration": { "$ref": "https://local-video.invalid/schema/director-preconfiguration.json#/$defs/v1" }, "migrationWarnings": { "type": "array", "items": { "type": "string", "maxLength": 300 }, "maxItems": 8 } } } }
  }
}

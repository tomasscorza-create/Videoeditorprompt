export function projectFingerprint(project: unknown): string;
/** Revisión de lo que puede cambiar una duración medida; ignora todo lo visual. */
export function projectTimingFingerprint(project: unknown): string;
